<section class="section-spacing">
  <div class="page-width empty-state">
    <h1>{{ 'templates.404.title' | t }}</h1>
    <p>{{ 'templates.404.subtext' | t }}</p>
    <a class="button" href="{{ routes.root_url }}">{{ 'templates.404.link' | t }}</a>
  </div>
</section>

{% schema %}
{ "name": "404" }
{% endschema %}
